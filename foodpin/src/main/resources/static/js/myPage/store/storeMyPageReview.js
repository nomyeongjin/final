const replyUpdateBtn = document.querySelectorAll(".reply-update-btn");
const replyDeleteBtn = document.querySelectorAll(".reply-delete-btn");

replyDeleteBtn.forEach((btn) => {

  btn.addEventListener("click", () => {

    
  });


})
